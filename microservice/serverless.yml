service: task-microservice
frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs20.x

functions:
  api:
    handler: app.handler
    events:
      - http:
          method: any
          path: /
      - http:
          method: any
          path: /{proxy+}

plugins:
  - serverless-offline
